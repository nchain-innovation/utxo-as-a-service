[package]
name = "uaas"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# if dowloading blocks >32 MB use the submodule rust-sv:
chain-gang = { path = "../../chain-gang" }

serde = { version = "1.0", features =["derive"] }
toml = "0.5.8"
lazy_static = "1.4.0"
serde_json = "1.0"
mysql = "*"
chrono = "0.4"
rand = "0.8.5"
hex = "0.4.3"
regex = "1"
retry = "1.3.1"
actix-web = "4.3.1"
signal-hook = "0.3.15"
