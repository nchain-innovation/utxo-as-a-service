
# Rust compiler output
/rust/target
/rust/src/target

# Compiled Python bytecode
*.py[cod]
__pycache__

# uv virtual environment
.venv

# Generated data
data/*.dat
data/postgres
# The MariaDB data directory a pre-migration stack left behind. Untracked but
# not ignored, so `git add data/` swept ~200 binary files — including the .ibd
# table data — into the index. This repository is public.
data/mysql/
data/dynamic.toml
data/dynamic*.toml
# Generated by the startup smoke test from data/uaasr.docker.toml
data/uaasr.smoke.toml

# Test symlink created by python/tests/conftest.py (Docker-style ../data layout)
python/data

out.txt
.vscode

# Secrets / local environment
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# macOS
.DS_Store